<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover" />
  <meta name="theme-color" content="#0066cc" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <meta name="format-detection" content="telephone=no" />
  
  <title><%= title %></title>
  <meta name="description" content="현대WNP - 대한민국 NO.1 세탁전문기업. 전문 세탁 서비스와 창업 기회를 제공합니다." />
  <meta name="keywords" content="현대WNP, 세탁, 창업, 전문세탁, 헬스세탁, 사우나세탁" />
  <meta name="author" content="현대W&P" />
  
  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website" />
  <meta property="og:title" content="<%= title %>" />
  <meta property="og:description" content="현대WNP - 대한민국 NO.1 세탁전문기업. 전문 세탁 서비스와 창업 기회를 제공합니다." />
  <meta property="og:url" content="<%= typeof baseUrl !== 'undefined' ? baseUrl : '' %>" />
  <meta property="og:site_name" content="현대W&P" />
  <% const logoUrl = typeof baseUrl !== 'undefined' ? baseUrl + '/favicon.png' : '/favicon.png'; %>
  <meta property="og:image" content="<%= logoUrl %>" />
  <meta property="og:image:width" content="180" />
  <meta property="og:image:height" content="180" />
  <meta property="og:image:type" content="image/png" />
  
  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image" />
  <meta property="twitter:title" content="<%= title %>" />
  <meta property="twitter:description" content="현대WNP - 대한민국 NO.1 세탁전문기업" />
  <meta property="twitter:image" content="<%= logoUrl %>" />
  
  <!-- Favicon (버전 쿼리로 캐시 무효화 - 시간까지 포함) -->
  <% const faviconVersion = Date.now(); %>
  <link rel="icon" type="image/x-icon" href="/favicon.ico?v=<%= faviconVersion %>" />
  <link rel="icon" type="image/png" href="/favicon.png?v=<%= faviconVersion %>" />
  <link rel="apple-touch-icon" sizes="180x180" href="/favicon.png?v=<%= faviconVersion %>" />
  <link rel="manifest" href="/site.webmanifest?v=<%= faviconVersion %>" />
  
  <!-- Performance optimizations -->
  <link rel="preconnect" href="//fonts.googleapis.com">
  <link rel="preconnect" href="//fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="//fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap">
  
  <link rel="stylesheet" href="/css/hyundai-wnp.css?v=<%= typeof version !== 'undefined' ? version : new Date().toISOString().slice(0,10).replace(/-/g,'') %>" />
  <script defer src="/js/hyundai-wnp.js?v=<%= typeof version !== 'undefined' ? version : new Date().toISOString().slice(0,10).replace(/-/g,'') %>"></script>
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a class="brand" href="/">현대W&P</a>
      <nav class="nav">
        <a href="/about">회사소개</a>
        <a href="/service">서비스 안내</a>
        <a href="/facility">시설현황</a>
        <a href="/estimate">세탁견적</a>
        <a href="/contact">고객센터</a>
        <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
          <div class="admin-menu">
            <a href="/admin" class="admin-link">대시보드</a>
            <a href="/admin/gallery" class="admin-link">갤러리 관리</a>
            <form method="POST" action="/admin/logout" class="logout-form">
              <button type="submit" class="btn-logout">로그아웃</button>
            </form>
          </div>
        <% } %>
      </nav>
      <button class="nav-toggle" aria-label="메뉴 열기">☰</button>
    </div>
  </header>
  <main>
